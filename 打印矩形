#include<stdio.h>
int main()
{
    int a,b;
    int c,d;
    printf("高度:");
    scanf("%d",&c);
    printf("宽度:");
    scanf("%d",&d);
    for(a=1;a<=10;a++)
    {
        for(b=1;b<=10;b++)
        {
            putchar('*');
        }
        putchar('\n');
    }
    return 0;
}
